spring:
  application:
    name: dispatch-engine

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

  datasource:
    url: jdbc:postgresql://localhost:5433/vektor
    username: vektor
    password: vektor
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000

  flyway:
    enabled: true
    out-of-order: false
    baseline-on-migrate: true # Tells Flyway it's okay to start fresh
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: dispatch-processor-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      # ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        # Delegate the actual parsing back to the JsonDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "com.vektor.dispatch_engine.*"
        spring.json.value.default.type: com.vektor.dispatch_engine.dto.deliveryevent.request.DeliveryEventUpdateRequest
        spring.json.use.type.headers: false
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - prometheus
          - metrics
  metrics:
    tags:
      application: dispatch-engine

  endpoint:
    health:
      show-details: always

  prometheus:
    metrics:
      export:
        enabled: true

resilience4j:
  circuitbreaker:
    instances:
      bankGateway:
        sliding-window-size: 5 # Look at the last 5 calls
        failure-rate-threshold: 50 # If 50% (or more) fail, trip the breaker!
        wait-duration-in-open-state: 15s # Wait 15 seconds before trying the bank again
        permitted-number-of-calls-in-half-open-state: 2 # Try 2 times before giving up
        minimum-number-of-calls: 3 # Minimum number of calls to trip the breaker
  ratelimiter:
    instances:
      bankGateway:
        limit-for-period: 10 # Allow max 10 calls per refresh period
        limit-refresh-period: 1s # Refresh permissions every 1 second
        timeout-duration: 0s # If no permissions available immediately, throw RequestNotPermitted without waiting

vektor:
  bank:
    base-url: ${VEKTOR_BANK_BASE_URL:http://localhost:8085}
  payout:
    schedule-ms: 60000
    partition-count: 4
  outbox:
    stuck-threshold-minutes: 5
    sweep-ms: 60000
  scheduling:
    enabled: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
